# Resend — required for /contact form submissions.
# Get a key at https://resend.com (free tier: 3,000 emails/mo, no CC).
# Add this variable in Vercel → Project → Settings → Environment Variables.
RESEND_API_KEY=
